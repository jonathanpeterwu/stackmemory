---
description: "Show StackMemory status and context"
allowed-tools: ["Bash(stackmemory status:*)"]
---

# StackMemory Status

Run the status command to see the current StackMemory state:

```!
stackmemory status
```

This shows:
- Current session info
- Active context frames
- Database statistics
- Recent activity
