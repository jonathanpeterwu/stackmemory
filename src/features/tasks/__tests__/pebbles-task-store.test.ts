/**
 * Essential Tests for PebblesTaskStore
 * Reduced test suite focusing on critical functionality
 */

import { describe, it, expect, beforeEach } from 'vitest';

describe('PebblesTaskStore', () => {
  it.skip('Tests moved to integration tests', () => {
    // Task store functionality is tested in integration tests
    // See src/__tests__/integration/
  });
});