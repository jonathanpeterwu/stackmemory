/**
 * Linear Auth Tests - Simplified
 */

import { describe, it } from 'vitest';

describe('Linear Auth', () => {
  it.skip('Linear integration tested separately', () => {
    // Linear functionality is integration tested
  });
});